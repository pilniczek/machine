import { StyleSheet } from 'aphrodite/no-important'


const styles = StyleSheet.create({
  svg: {
    width: '100%',
    shapeRendering: 'geometricPrecision',
    backgroundColor: '#5e5770',
    stroke: 'rgba(0,0,0,.4)',
    strokeWidth: .5,
  }
});

export default styles;
